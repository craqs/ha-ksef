{
  "config": {
    "step": {
      "user": {
        "title": "Connect to KSeF",
        "description": "Enter your KSeF credentials. Generate a token in the KSeF portal under Account → API Tokens.",
        "data": {
          "nip": "NIP (tax ID)",
          "token": "KSeF Token",
          "use_prod": "Use production environment"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to KSeF API. Check your internet connection.",
      "invalid_auth": "Invalid credentials. Verify your NIP and KSeF token.",
      "unknown": "Unexpected error. See Home Assistant logs for details."
    },
    "abort": {
      "already_configured": "This NIP is already configured."
    }
  }
}
